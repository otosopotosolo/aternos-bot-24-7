# CORS Misconfiguration on cdn77

## Report Information
- **Platform:** HackerOne
- **Program:** cdn77
- **Severity:** CRITICAL
- **Date:** 2026-06-07
- **Target:** https://api.cdn77.com/profile

---

## Summary

A CORS misconfiguration was discovered in the cdn77 API. The API endpoint allows requests from arbitrary origins and, in some cases, supports credentials.

---

## Steps to Reproduce

1. Send a preflight OPTIONS request:
```bash
curl -I -X OPTIONS 'https://api.cdn77.com/profile' \
  -H 'Origin: https://evil-test-cors.com' \
  -H 'Access-Control-Request-Method: GET' \
  -H 'Access-Control-Request-Headers: Authorization,Content-Type'
```

2. Observe the response headers:
```
Access-Control-Allow-Origin: *
Access-Control-Allow-Credentials: true
Access-Control-Allow-Methods: 
```

---

## Evidence

| Header | Value |
|--------|-------|
| Access-Control-Allow-Origin | * |
| Access-Control-Allow-Credentials | true |
| Access-Control-Allow-Methods |  |
| HTTP Status Code | 200 |

---

## Impact

CRITICAL severity - Arbitrary origin allowed with credentials enabled. An attacker can:
- Steal sensitive user data via JavaScript fetch
- Perform actions on behalf of logged-in users
- Access authentication credentials/cookies

---

## Remediation

1. Use a strict whitelist of allowed origins
2. Don't use `*` wildcard for `Access-Control-Allow-Origin` with `Access-Control-Allow-Credentials: true`
3. Validate Origin header server-side
4. Use CORS properly configured for your specific domain(s)

---

## References

- CWE-346: Origin Validation Error
- CWE-942: Permissive Cross-Domain Policy

---

*Report generated by SARAhack Mass CORS Scanner on 2026-06-07T15:29:14.344110*
